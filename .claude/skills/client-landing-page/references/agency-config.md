# Agency config

One-time settings for the client-landing-page skill. Check this file before assembling a page (Step 5). If any value below is still the placeholder, ask the user for the real value once, use it, and then edit this file so future runs don't ask again.

- **Calendly URL**: `PLACEHOLDER — ask the user for their Calendly scheduling link (e.g. https://calendly.com/aventio/intro-call)`
- **Sender/agency signature for emails**: `PLACEHOLDER — ask the user how they want to sign off (agency name, their name, or both)`
- **Agency site/domain (used as `embed_domain` for the Calendly iframe)**: `PLACEHOLDER — ask the user, or default to the domain they're sending the pitch page from`
